class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        sathwik={}
        O=[]
        for num in nums:
            sathwik[num]=sathwik.get(num,0)+1
        for k in sathwik:
            if sathwik[k]>len(nums)//3:
                O.append(k)
        return O

        
