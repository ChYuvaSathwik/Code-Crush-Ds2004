class Solution:
    def secondHighest(self, s: str) -> int:
        sathwik=[]
        for i in s:
            if i.isdigit():
                sathwik.append(int(i))
        Unique=list(set(sathwik))
        hi=sorted(Unique,reverse=True)
        if len(hi)<2:
            return -1
        else:
            return hi[1]
