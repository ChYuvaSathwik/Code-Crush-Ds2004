class Solution:
    def isMonotonic(self, nums: List[int]) -> bool:
        A=sorted(nums,reverse=True)
        B=sorted(nums,reverse=False)
        if nums==A or  nums==B:
            return True
        else:
            return False
