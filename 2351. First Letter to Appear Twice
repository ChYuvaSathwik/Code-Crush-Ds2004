class Solution:
    def repeatedCharacter(self, s: str) -> str:
        sathwik=set()
        for num in s:
            if num in sathwik:
                return num
            sathwik.add(num)
            






        
